{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:docs": {
      "outputs": ["storybook-static/**"]
    },
    "build": {
      "outputs": ["dist/**"]
    },
    "publish": {}
  }
}