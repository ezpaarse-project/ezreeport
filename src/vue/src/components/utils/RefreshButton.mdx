import { Meta, Story } from '@storybook/blocks';

import * as RefreshButtonStories from './RefreshButton.stories';

<Meta of={RefreshButtonStories} />

# Refresh Button

Wrapper of a `v-button` inside a `v-tooltip`.

## Usage

<Story of={RefreshButtonStories.Basic} />