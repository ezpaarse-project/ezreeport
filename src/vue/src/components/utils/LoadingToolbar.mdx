import { Meta, Story } from '@storybook/blocks';

import * as LoadingToolbarStories from './LoadingToolbar.stories';

<Meta of={LoadingToolbarStories} />

# LoadingToolbar

Wrapper of `v-toolbar` with a `v-progress-linear`.

## Usage

<Story of={LoadingToolbarStories.Basic} />

You can specify custom content in the slot

<Story of={LoadingToolbarStories.CustomContent} />